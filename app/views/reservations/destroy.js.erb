(function () {
  var sel = '#reservation-' + <%= @reservation.id %>;
  if ($(sel).siblings('li').length == 0) {
    $(sel).replaceWith('<li class="empty">No reservations.</li>');
  }
  else {
    $(sel).remove();
  }
})()
