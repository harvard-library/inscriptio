<li class="reservation_notice line" id="reservation_notice-<%= reservation_notice.id %>">
  <%= link_to("#{reservation_notice.status} - #{reservation_notice.subject}", reservation_notice ) %>

  <% if can? :update, reservation_notice %>
	<ul class="actions">
      <li><%= link_to('Edit', edit_reservation_notice_path(reservation_notice), :class => "action-edit") %></li>
    </ul>
  <% end %>
</li>
